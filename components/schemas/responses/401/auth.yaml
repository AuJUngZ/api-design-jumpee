login-resp-401:
  type: object
  properties:
    message:
      type: string
      example: 'Login failed'
    status:
      type: string
      example: 401 Unauthorized

update-password-resp-401:
    type: object
    properties:
      message:
        type: string
        example: 'Change password failed'
      status:
        type: string
        example: 401 Unauthorized